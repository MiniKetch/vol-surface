// Defines doctest's main(). Keep this in its own TU so the heavy
// implementation header is parsed exactly once — speeds up rebuilds.
#define DOCTEST_CONFIG_IMPLEMENT_WITH_MAIN
#include <doctest/doctest.h>
